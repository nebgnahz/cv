language: ruby
sudo: required

addons:
  apt:
    packages:
      - make
      - texlive-fonts-extra
      - texlive-fonts-recommended
      - texlive-generic-extra
      - texlive-latex-extra
      - texlive-math-extra
      - texlive-pictures
      - texlive-pictures
      - texlive-science
      - texlive-science
      - texlive-xetex

script:
  - make
  - mkdir -p pdf
  - mv cv.pdf pdf/

branches:
  only:
  - master

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN
  local_dir: pdf
  on:
    branch: master
